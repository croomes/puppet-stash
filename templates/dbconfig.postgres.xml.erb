<?xml version="1.0" encoding="UTF-8"?>
 
<stash-database-config>
  <name>defaultDS</name>
  <delegator-name>default</delegator-name>
  <database-type>postgres72</database-type>
  <schema-name>public</schema-name>
  <jdbc-datasource>
    <url><%= scope.lookupvar('stash::params::dburl') %></url>
    <driver-class>org.postgresql.Driver</driver-class>
    <username><%= scope.lookupvar('stash::params::dbuser') %></username>
    <password><%= scope.lookupvar('stash::params::dbpassword') %></password>
    <pool-size>15</pool-size>
  </jdbc-datasource>
</stash-database-config>